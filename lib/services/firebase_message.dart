// import 'package:firebase_messaging/firebase_messaging.dart';

class AppFcm {
  static subscribeToTopic(String topic) {
    // FirebaseMessaging.instance.subscribeToTopic(topic).then((value) {
    //   print('FCM: Subscribe to user chanel $topic');
    // });
  }

  static unsubscribeFromTopic(String topic) {
    // FirebaseMessaging.instance.unsubscribeFromTopic(topic).then((value) {
    //   print('FCM: Unsubscribe from user chanel $topic');
    // });
  }

  static deleteInstanceId() {
    // print('FCM: deleteInstanceId');
    // FirebaseMessaging.instance.deleteToken();
  }
}
